openapi: 3.2.0
info:
  title: Offer Tool Site API
  description: Api definitions for Offer Tool
  version: '1.0'
  contact: {}
servers: []
tags:
- name: Site
paths:
  /api/v1/customers/{customerId}/sites:
    post:
      operationId: CreateCustomerSiteController_handle_v1
      parameters:
      - name: customerId
        required: true
        in: path
        schema:
          type: number
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateCustomerSiteRequestDto'
      responses:
        '201':
          description: Created customer site
      tags:
      - Site
      summary: Create customer site controller handle v1
      x-summary-source: derived
components:
  schemas:
    CreateCustomerSiteRequestDto:
      type: object
      properties: {}
